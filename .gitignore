eval_logs/
edit_logs/
briefs/
__pycache__/
*.pyc
.env
.venv/
typeset/*.pdf
typeset/chapters_content.tex
*.pdf

# build artifacts
*.epub
build/*.png
build/*.md
cover.png
build/*.jpg
build/*.txt
build/*.tex
substack/cards/*.png
substack_cookies.json
substack/preview/

# press-kit build artifact (regenerated by social/make_presskit.sh, hosted on GCS)
books/the-last-human-ceo/social/press-kit.zip

# --- Local cruft ---
.DS_Store
.claude/

# --- Regenerable audiobook media (rebuildable from scripts; masters hosted on GCS) ---
# Keep: PRODUCTION_SPEC.md, cover.jpg, scripts/*.json, intros/*.txt, recaps/*
books/*/audiobook/produced/
books/*/audiobook/chapters/
books/*/audiobook/*.mp3
books/*/audiobook/*.wav
books/*/audiobook/**/*.mp3
books/*/audiobook/**/*.wav
books/*/audiobook/*.log

# --- Regenerable social video (bundled into press-kit.zip on GCS) ---
books/*/social/video/

# --- Substack automation runtime state / credentials (never commit) ---
books/*/substack/draft_ids.json
**/substack_cookies.json
